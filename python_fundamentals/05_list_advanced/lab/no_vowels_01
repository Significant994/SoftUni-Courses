# text = input()
vowels = ['a', 'o', 'u', 'e', 'i']

new_text = [letter for letter in input() if letter.lower() not in vowels]
# for letter in text:
#     if letter.lower() not in vowels:
#         new_text.append(letter)

print(''.join(new_text))
