number_n = int(input())
start_index = 97

for i in range(start_index,start_index + number_n):
    for j in range(start_index, start_index + number_n):
        for k in range(start_index, start_index + number_n):
            print(f"{chr(i)}{chr(j)}{chr(k)}")
