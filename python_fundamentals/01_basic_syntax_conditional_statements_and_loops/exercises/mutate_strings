first_string = input()
second_string = input()

for index in range(len(first_string)):
    if first_string[index] == second_string[index]:
        continue
    print(second_string[:index+1] + first_string[index+1:])
